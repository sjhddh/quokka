# Quokka Companion App Configuration
# Copy this file to .env and adjust values as needed.

# Port the companion HTTP server listens on (default: 7749)
QUOKKA_PORT=7749

# Path to the SQLite database file (default: quokka.db in working directory)
QUOKKA_DB_PATH=quokka.db
